#include <mars.h>

void init_map(int width, int height, CELL *map)
{
	for (int y = 0; y < height; y++)
	{
		if (y == 0 || y == height-1)
		{
			for (int x = 0; x < width; x++)
				map[x][y].type = ROCK;
		}

		else
		{
			map[0][y].type = ROCK;
			map[width-1][y].type = ROCK;
		}
	}
}
